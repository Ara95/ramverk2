extend layout.pug

block main
    main.edit
    h1 Edit
    form(method='post', action='/update')
        input(type='hidden' name='id', value= item.id)
        input(name='brand', value= item.brand)
        input(name='price', value= item.price)
        input(name='year', value= item.year)
        button Update
